{
  "name": "trend-hunter",
  "version": "1.0.0",
  "description": "AI-powered trend-to-content engine with an Avalanche Fuji trend registry",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:fuji": "hardhat run scripts/deploy.js --network fuji",
    "verify": "hardhat verify --network fuji",
    "node": "hardhat node",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.0",
    "ethers": "^6.11.0",
    "express": "^4.18.0",
    "axios": "^1.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
